# pyproject.toml
[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"


dependencies = [
    "numpy>=1.0.0",
    "GPUtil>=1.0.0"
    "requests>=2.0.0",
    "pandas>=1.3.0",
    "requests>=2.25.1",
    "torch>=1.0.0"    
]
