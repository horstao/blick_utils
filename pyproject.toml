[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "BlickUtils"
version = "0.2.0"
description = "A collection of utility functions for Blick Technologies"
authors = [{ name = "Your Name", email = "your@email.com" }]
dependencies = [
    "numpy>=1.0.0",
    "GPUtil>=1.0.0"
    "requests>=1.0.0",
    "pandas>=1.0.0",
    "requests>=2.0.0",
    "torch>=1.0.0"
]